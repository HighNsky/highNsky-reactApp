import { EndPoint } from "../../../types/endpoints";

const formLinkBooking: EndPoint = {
  booking: {
    uri: "/formLinkBooking",
    method: "POST",
    version: "",
  },
  
};

export default formLinkBooking;