import { EndPoint } from "../../../../types/endpoints";
const rideEndpoints: EndPoint = {
    signup : {
    uri: "/passenger/signup",
    method: "POST",
    version: "",
  },
};

export default rideEndpoints;
