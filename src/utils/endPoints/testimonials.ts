import { EndPoint } from "../../../types/endpoints";

const testimonialsEndPoints: EndPoint = {
  getTestimonials: {
    uri: "/testimonials/",
    method: "GET",
    version: "",
  },
 
};

export default testimonialsEndPoints;
