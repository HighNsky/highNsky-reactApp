import { EndPoint } from "../../../types/endpoints";

const vehicleOption: EndPoint = {
  getVehicleOption: {
    uri: "/vehicleOption/",
    method: "GET",
    version: "",
  },
  
};

export default vehicleOption;
