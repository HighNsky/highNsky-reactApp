import { atom } from "jotai";
export const bookingData= atom({});
export const booking = atom({});
export const vehiclOp = atom([]);
export const vehiclePrice = atom({ priceDetail: { price: 0, priceTitle: "" } });
export const vehiclOptionPrice = atom({});
export const meUser = atom([]);
